#particles { position: absolute; width: 100%; height: 100%; background: radial-gradient(circle, #1a001a 0%, #000 100%); z-index: 1; }
@keyframes pulse { 0% { transform: scale(1); } 50% { transform: scale(1.1); } 100% { transform: scale(1); } }
#brain-core { animation: pulse 3s infinite; }
